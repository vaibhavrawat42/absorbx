// This project now uses Next.js. Run:
//   npm run dev   — development server (http://localhost:3000)
//   npm run build && npm start   — production
console.log('This project uses Next.js. Run: npm run dev');
process.exit(1);
